To use the Scrape-DOM API a user have to add a body with the correct format (exmpale exists in RequestBody.json)
with the POST request in correct order you can scrape any element on any website you want.

If the website you want to scrape have elements that are dynamiclly loaded you should use puppeteer headless browser to scrape
